<?php

/*$contacts = ["pepe", "antonio", "nate"]; /*Usamos el símbolo $ para crear una variable*/

/*foreach ($contacts as $contact) { /*Creamos un bucle for in, pero en este caso en PHP se usa foreach
  print("<div>$contact</div>" . PHP_EOL); 
  /*Imprimimos un HTML con un div, es decir, generamos un HTML dinámico en base a una varible. 
  * Si ponemos un$ dentro de una cadena con dobles comillas se interpreta la variable 
  * Si fueran comillas simples se interpretaría todo como texto
  * Para concatenar en vez de usar el "+" como en Python se uyiliza el "."
  * Para genera un salto de linea en vez de usar el "\n" como en Python se utiliza el PHP_EOL
}*/

/*La gente se dio cuenta que usar tantos prints para crear HTML era muy tedioso, por eso se utilizó el <?php ... ?>
* todo lo que que queda encerrado dentro de ese <?php ... ?> será PHP y el resto será HTMl directamente y se convertirá en un print*/




